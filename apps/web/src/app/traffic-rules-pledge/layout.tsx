import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pune Traffic Rules Pledge | I Will Follow All Traffic Rules",
  description: "Take the Citizens First Pune traffic-rules pledge and encourage more Pune citizens to follow safe and responsible traffic practices."
};

export default function PledgeLayout({ children }: { children: React.ReactNode }) { return children; }
